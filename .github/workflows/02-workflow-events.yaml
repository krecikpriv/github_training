---
name: 02 – Workflow Events
on:
  workflow_dispatch:
  pull_request:
  schedule:
    - cron: '0 14 * * *'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: echo "Zostałem uruchomiony przez zdarzenie ${{github.event_name}}"