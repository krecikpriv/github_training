---
name: 04-01 - Using Actions
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 04-praca-z-zewnetrzymi-akcjami/react-app
    steps:
      - name: Checkout code
        uses: actions/checkout@v4